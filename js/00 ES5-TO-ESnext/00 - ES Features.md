#### ES5 | ES2009
```
    "use strict"
    String[number] access
    Multiline strings
    String.trim()
    Array.isArray()
    Array.forEach()
    Array.map()
    Array.filter()
    Array.reduce()
    Array.reduceRight()
    Array.every()
    Array.some()
    Array.indexOf()
    Array.lastIndexOf()
    JSON.parse()
    JSON.stringify()
    Date.now()
    Date.toISOString()
    Date.toJSON()
    Property getters and setters
    Reserved words as property names
    Object methods
    Object defineProperty()
    Function.bind()
    Trailing commas
```

#### ES6 | ES2015
```
    The let keyword
    The const keyword
    Arrow Functions
    For/of
    Map Objects
    Set Objects
    Classes
    Promises
    Symbol
    Default Parameters
    Function Rest Parameter
    String.includes()
    String.startsWith()
    String.endsWith()
    Array.from()
    Array.keys()
    Array.find()
    Array.findIndex()
    New Math Methods
    New Number Properties
    New Number Methods
    New Global Methods
    Iterables Object.entries
    JavaScript Modules
```

#### ES7 | ES2016
```
    01) Exponentiation Operator
    02) Array includes() | array.includes(element, ?fromIndex)
```

#### ES8 | ES2017
```
    01) Padding a String
    02) Object.values() | Object.entries()
    03) async | await
```

#### ES9 | ES2018
```
    01) Object spread operator
    02) Promise.prototype.finally()
    03) Asynchronous iterators
    04) Async generators
```

#### ES10 | ES2019
```
    01) Array flat()
    02) Array flatMap()
    03) Object.fromEntries() | convert a list of key-value pairs into an object
    04) String trimStart()
    05) String trimEnd()
    06) Optional catch binding
```

#### ES11 | ES2020
```
    01) Nullish Coalescing Operator | name = (undefined || null) ?? "Jhon"
    02) Optional Chaining Operator  | avatar = user ?. profile ?. avatar;
    03) Promise.allSettled()
    04) Dynamic import
    05) BigInt(100) | 100n
    06) globalThis | (browser[window, frames], WebWorkersAPI[self], NodeJS[global])
```

#### ES12 | ES2021
```
    01) String replaceAll
    02) Logical Assignment Operators | (||=, &&=, ??=)
    03) Numeric Separator | 10_10_10 === 101010
    04) Promise.any() | return the first Promise that fulfills
```
