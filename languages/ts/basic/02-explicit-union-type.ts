// Explicit Type
const person: string = "Rahim";
const isAdult: boolean = true;



// Union Type
let mixed: (string | number | boolean)[] = [];
mixed.push(10);
mixed.push("string");
mixed.push(true);
